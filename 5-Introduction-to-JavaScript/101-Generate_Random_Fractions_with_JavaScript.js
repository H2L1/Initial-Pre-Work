// Learned to use the Math.random() method to randomly generate a number between 0 and almost 1

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
