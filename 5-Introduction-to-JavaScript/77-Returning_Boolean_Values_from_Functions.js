// LEarned to simplify code using boolean expressions as the return value instead of using if statements for a true and a false

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
