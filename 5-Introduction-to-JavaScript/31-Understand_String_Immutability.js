// Learned that strings are immutable so they can't be changed with bracket notation, you must reassign the string value stored in the variable to change setInterval(function () {


// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
