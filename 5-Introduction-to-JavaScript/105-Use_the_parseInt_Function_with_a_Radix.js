// LEarned to use the parseInt function and call it with a radix which tells the computer which base number system the value needs to be parsed in.

function convertToInteger(str) {
  return parseInt(str, 2);
}

convertToInteger("10011");
