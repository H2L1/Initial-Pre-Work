// LEarned to use the quality operator to compare two variables and returns true or trueOrFalse
// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
