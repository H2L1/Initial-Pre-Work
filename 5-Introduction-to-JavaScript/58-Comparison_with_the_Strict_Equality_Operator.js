// LEarned to use the strict equality operator to check if two items are the exact same. Does not preform type conversion

// Setup
function testStrict(val) {
  if (val === 7) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testStrict(10);
